#include "main.h"
/**
 * _strcmp - this function concatnates two strings
 *
 * @s1: dest string
 * @s2: src string
 *
 * Return: 0
 */
char *cap_string(char *s)
{
	int i;
	int tmp;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i - 1] == " ")
		{
			return = s[i + 32];
		}
	}
}
